import { TimespanToTicksPipe } from './timespan-to-ticks-pipe';

describe('TimespanToTicksPipe', () => {
  it('should create an instance', () => {
    expect(new TimespanToTicksPipe()).toBeTruthy();
  });
});
